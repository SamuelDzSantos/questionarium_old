server:
  port: 14000

spring:
  application:
    name: gateway

  # Quando rodar localmente, o Postgres está em container, mas exposto no host em localhost:10000
  datasource:
    url: jdbc:postgresql://localhost:10000/auth
    username: admin
    password: Admin_123
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create-drop

  # Se o Gateway precisar ler do Mongo (mesmo que seja só para testes):
  data:
    mongodb:
      uri: mongodb://admin:Admin_123@localhost:10002/auth_db2

  # Se o Gateway precisar de RabbitMQ (por exemplo para publicar mensagens):
  rabbitmq:
    host: localhost
    port: 5672
    username: admin
    password: admin

logging:
  level:
    org:
      springframework:
        security: DEBUG
      data:
        mongodb:
          core:
            MongoTemplate: DEBUG
            ReactiveMongoTemplate: DEBUG
      mongodb:
        driver:
          protocol:
            command: DEBUG

key:
  secret: 1231dwjaodij1023aowdijao10woiajdowiaj109diajwoijaodl10wodiajlijzlkscjlzkjclkajclawkjdldfwel
