spring:
    application:
        name: eureka

server:
  port: 8083

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://${eureka.uri:localhost:8083}/eureka/
